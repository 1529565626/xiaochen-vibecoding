1、搭建一个电脑端运行的文件管理服务，类似于网盘，但是只在局域网运行，文件的访问和下载上传，通过网页访问
2、服务端管理采用网页形式管理查看，客户端访问也是
3、实现上采用主流的代码实现
4、单个文件大小上传下载保证在10G，批量20G，配置内容可以进行调整，提供页面进行编辑配置，方便电脑配置不足是进行调整
5、创建一个本地域名映射，仅在局域网内有效，方便收藏访问

6、文件保存的路径可以进行配置，需确保访问时不会有权限问题
7、文件的上传和下载提供默认目录，交互页面上进行隔离访问
8、管理端可以进行文件夹新增、更名、删除

根据以上需求，先生成一份方案报告，告诉我哪些内容实现上比较复杂，有好的替代方案也可以说明，我确认无误后生成一份新的需求报告，配置需求等初始参数配置写入一份新的文档，我进行补充，方便后续项目生成。 